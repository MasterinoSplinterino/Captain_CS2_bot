# 📄 Техническое Задание: Telegram-бот для управления CS2-сервером

## 1. Общие сведения

| Параметр | Значение |
| :--- | :--- |
| **Наименование проекта** | Telegram-бот для удаленного управления сервером CS2. |
| **Цель проекта** | Создание удобного интерфейса на базе Telegram для выполнения административных действий на выделенном CS2-сервере (сборка `kus/cs2-modded-server`). |
| **База сервера** | `https://github.com/kus/cs2-modded-server` |
| **Протокол управления** | RCON (Remote Console). |
| **Среда хостинга** | Домашний сервер заказчика. |
| **Внешний доступ** | Cloudflare Tunnel. |

## 2. Архитектура системы

Система состоит из трех ключевых модулей, работающих на домашнем сервере:

1.  **CS2 Server:** Запускает и поддерживает игру. Должен иметь включенный и настроенный RCON-доступ.
2.  **Telegram Bot Application:** Обрабатывает входящие команды из Telegram и преобразует их в RCON-запросы.
3.  **Cloudflare Tunnel:** Обеспечивает безопасный доступ игроков к игровому порту сервера (TCP/UDP, обычно 27015).

## 3. Требования к функционалу Telegram-бота

Бот должен быть реализован с ограничением доступа (только для авторизованного Telegram ID) и обеспечивать следующий функционал:

### 3.1. Управление картой

* **Триггер:** Команда `/map` (или кнопка "Сменить карту").
* **Действие:** Бот должен вывести сообщение с **инлайн-кнопками**, каждая из которых соответствует одной карте из заранее определенного каталога.
* **Каталог карт (Примеры, требующие верификации):**
    * `de_mirage`
    * `de_dust2`
    * `de_inferno`
    * `de_nuke`
    * `de_anubis`
    * `cs_office`
* **Команда RCON:** При нажатии кнопки отправляется команда `changelevel <название_карты>`.

### 3.2. Управление режимом игры (Changing game modes)

* **Триггер:** Команда `/mode` (или кнопка "Сменить режим").
* **Действие:** Бот должен вывести сообщение с **инлайн-кнопками**, каждая из которых соответствует одному режиму игры.
* **Каталог режимов:**

| Название кнопки | game\_type | game\_mode | Команда RCON |
| :--- | :--- | :--- | :--- |
| **Casual (Обычный)** | 0 | 0 | `game_type 0; game_mode 0; map <текущая/выбранная_карта>` |
| **Competitive (Соревновательный)** | 0 | 1 | `game_type 0; game_mode 1; map <текущая/выбранная_карта>` |
| **Deathmatch** | 2 | 0 | `game_type 2; game_mode 0; map <текущая/выбранная_карта>` |
| **Wingman (Напарники)** | 3 | 0 | `game_type 3; game_mode 0; map <текущая/выбранная_карта>` |

* **Команда RCON:** При нажатии кнопки отправляется последовательность команд для установки типа/режима и перезагрузки текущей карты для активации нового режима.

### 3.3. Управление ботами и игроками

| Функция | Триггер | Параметры | Команда RCON |
| :--- | :--- | :--- | :--- |
| **Добавить бота (T)** | `/addt` | `<сложность> (опционально)` | `bot_add_t <difficulty>` |
| **Добавить бота (CT)** | `/addct` | `<сложность> (опционально)` | `bot_add_ct <difficulty>` |
| **Убрать всех ботов** | `/removebots` | Нет | `bot_kick` |
| **Кикнуть игрока** | `/kick` | `<имя_игрока> (обязательно)` | `kick <имя_игрока>` |
| **Получить статус** | `/status` | Нет | `status` (для получения списка игроков и текущих настроек) |

### 3.4. Команды для администратора

| Команда | Описание |
| :--- | :--- |
| `/start` | Приветственное сообщение. |
| `/help` | Список доступных команд и их описание. |
| `/restart` | Перезапуск текущего матча (RCON: `mp_restartgame 1`). |

## 4. Требования к реализации

### 4.1. Разработка Telegram-бота

* **Язык:** На выбор разработчика (рекомендуется Python или Node.js) с использованием стабильной библиотеки для работы с Telegram API и RCON-протоколом.
* **Обработка ошибок:** Бот должен корректно обрабатывать ошибки RCON-подключения (неправильный пароль, недоступность сервера) и информировать об этом пользователя.
* **Сообщения:** Все сообщения бота должны быть информативными и понятными.

### 4.2. Настройка RCON

* **Аутентификация:** RCON-клиент в боте должен использовать заранее установленный `rcon_password`.
* **Доступ:** Бот должен иметь сетевой доступ к RCON-порту CS2-сервера (предпочтительно локально, если бот и сервер на одном хосте).

### 4.3. Ограничение доступа

* Бот должен принимать команды только от заранее настроенного `USER_ID` (ID аккаунта Telegram).

## 5. Требования к развертыванию (Deployment)

1.  **Контейнеризация:** Приложение Telegram-бота должно быть подготовлено для развертывания в Docker-контейнере.
2.  **Запуск:** Бот должен запускаться как сервис на домашнем сервере (например, через Docker Compose или systemd).

## 6. Каталоги и Настройки

Все ключевые настройки должны быть вынесены в отдельный конфигурационный файл (например, `.env` или JSON), включая:

* `TELEGRAM_BOT_TOKEN`
* `RCON_HOST`
* `RCON_PORT`
* `RCON_PASSWORD`
* `ALLOWED_USER_ID`
* Список доступных **Карт**
* Список доступных **Режимов игры** (тип, режим и название для кнопки)