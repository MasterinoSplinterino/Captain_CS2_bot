services:
  bot:
    build:
      context: ./bot
      dockerfile: Dockerfile
    container_name: cs2-captain-bot
    env_file:
      - .env
    volumes:
      - bot-data:/data
    restart: unless-stopped

volumes:
  bot-data:
