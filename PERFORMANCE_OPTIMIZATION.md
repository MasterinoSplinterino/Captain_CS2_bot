# 🚀 Оптимизация производительности CS2 сервера

## 📋 Что было оптимизировано

### 1. Docker Compose (docker-compose.prod.yml)

#### Сетевой режим
- `network_mode: host` - контейнер работает в сети хоста напрямую
- Убирает прослойку Docker NAT для минимальных задержек
- Сервер работает с сетью напрямую, без виртуализации
- **Важно:** Сетевые параметры (BBR, буферы) настраиваются через [optimize_system.sh](optimize_system.sh) на хосте

#### Capabilities
- `SYS_NICE` - возможность изменять приоритет процессов
- `SYS_RESOURCE` - возможность изменять лимиты ресурсов

#### Память
- `memory: 4g` (limit) - максимум 4GB RAM
- `memory: 2g` (reservation) - гарантированно 2GB RAM
- Предотвращает OOM (Out of Memory) убийство процесса

#### CPU
- `cpus: '4'` (limit) - максимум 4 CPU ядра
- `cpus: '2'` (reservation) - гарантированно 2 CPU ядра

#### Безопасность и производительность
- `seccomp:unconfined` - отключение seccomp для минимальных задержек
- `ulimits` - увеличенные лимиты файловых дескрипторов и процессов

### 2. CS2 Server Config (через переменные окружения в Docker)

Все настройки прописаны в docker-compose.prod.yml через переменные окружения с префиксом `CS2_CFG_`

#### Network Tickrate
```
CS2_CFG_sv_maxupdaterate=128      // Максимум 128 tick для клиентов
CS2_CFG_sv_minupdaterate=64       // Минимум 64 tick
CS2_CFG_sv_maxcmdrate=128         // Команды от клиентов
CS2_CFG_sv_mincmdrate=64
```

#### Параллельная обработка
```
CS2_CFG_sv_parallel_packentities=1     // Параллельная упаковка сущностей
CS2_CFG_sv_parallel_sendsnapshot=1     // Параллельная отправка снапшотов
CS2_CFG_host_thread_mode=2             // Многопоточный режим
```

#### Prediction исправления
```
CS2_CFG_sv_lagcompensation_teleport_dist=64  // Дистанция телепортации для lag compensation
CS2_CFG_sv_clockcorrection_msecs=15          // Коррекция часов клиента
CS2_CFG_sv_maxunlag=0.4                      // Максимальная компенсация лага
```

### 3. System Optimization (optimize_system.sh)

Скрипт для оптимизации хост-системы VPS:

#### Kernel параметры
- BBR congestion control
- Увеличенные сетевые буферы
- Оптимизация TCP параметров
- Отключение transparent huge pages

#### Docker daemon
- Ограничение размера логов
- Оптимизация ulimits для контейнеров
- Storage driver overlay2

---

## 🎯 Как применить оптимизации

### Вариант 1: Автоматический (рекомендуется)

```bash
# 1. Закоммитьте изменения
git add .
git commit -m "feat: Add performance optimizations for CS2 server"
git push

# 2. На VPS запустите системную оптимизацию (один раз)
ssh user@your-vps-ip
cd /path/to/project
sudo chmod +x optimize_system.sh
sudo ./optimize_system.sh

# 3. Деплой через Dokploy
# Откройте панель Dokploy и нажмите Deploy
```

### Вариант 2: Ручной деплой

```bash
# На VPS
cd /path/to/project

# 1. Системная оптимизация
sudo chmod +x optimize_system.sh
sudo ./optimize_system.sh

# 2. Остановите контейнеры
docker-compose -f docker-compose.prod.yml down

# 3. Пересоберите и запустите
docker-compose -f docker-compose.prod.yml up -d --build

# 4. Проверьте логи
docker logs cs2-server -f
```

---

## 📊 Проверка результатов

### 1. Проверка сетевых параметров

```bash
# На хосте
sysctl net.ipv4.tcp_congestion_control
sysctl net.core.rmem_max
sysctl net.core.wmem_max

# Должны показать BBR и 16777216
```

### 2. Проверка Docker контейнера

```bash
# Использование ресурсов
docker stats cs2-server

# Сетевые параметры в контейнере
docker exec cs2-server sysctl net.ipv4.tcp_congestion_control
```

### 3. Мониторинг tick errors

```bash
# Следите за логами
docker logs cs2-server -f | grep -i "prediction\|unexpected"

# Если ошибки пропали - оптимизация сработала!
```

---

## 🔧 Тонкая настройка

### Если у вас 2 CPU ядра (слабый VPS)

Измените в [docker-compose.prod.yml](docker-compose.prod.yml):

```yaml
deploy:
  resources:
    limits:
      cpus: '2'
      memory: 2g
    reservations:
      cpus: '1'
      memory: 1g
```

### Если у вас 8+ CPU ядер (мощный VPS)

```yaml
deploy:
  resources:
    limits:
      cpus: '6'
      memory: 6g
    reservations:
      cpus: '4'
      memory: 4g
```

### Изменение tickrate сервера

В [docker-compose.prod.yml](docker-compose.prod.yml) измените переменные окружения:

```yaml
# Для 64 tick сервера
- CS2_CFG_sv_maxupdaterate=64
- CS2_CFG_sv_minupdaterate=64
- CS2_CFG_sv_maxcmdrate=64
- CS2_CFG_sv_mincmdrate=64

# Для 128 tick сервера (требует больше CPU!)
- CS2_CFG_sv_maxupdaterate=128
- CS2_CFG_sv_minupdaterate=128
- CS2_CFG_sv_maxcmdrate=128
- CS2_CFG_sv_mincmdrate=128
```

---

## ⚠️ Известные проблемы и решения

### Проблема: Ошибки при запуске Docker

**Ошибка:**
```
Error response from daemon: invalid ulimit type: nproc
```

**Решение:**
Удалите или закомментируйте секцию `ulimits` в docker-compose.prod.yml

### Проблема: Не применяются sysctls

**Ошибка:**
```
sysctl: setting key "net.core.rmem_max": Read-only file system
```

**Решение:**
Запустите [optimize_system.sh](optimize_system.sh) на хосте, а не в контейнере.

### Проблема: Все еще есть tick errors

**Возможные причины:**

1. **Недостаточно CPU** - проверьте `docker stats`, если CPU > 80% - нужен более мощный VPS
2. **Shared CPU VPS** - перейдите на Dedicated CPU VPS
3. **Проблемы сети** - проверьте ping и packet loss до VPS
4. **Много игроков** - уменьшите `sv_maxplayers`

**Дополнительные шаги:**

```bash
# 1. Уменьшите tickrate до 64 в docker-compose.prod.yml
# Измените CS2_CFG_sv_maxupdaterate и другие на 64

# 2. Увеличьте приоритет контейнера
docker update --cpus="4" --cpu-shares=1024 cs2-server

# 3. Проверьте нагрузку хоста
top
# Смотрите на CPU steal time (st) - должен быть < 5%
```

---

## 📈 Ожидаемые улучшения

После применения оптимизаций вы должны увидеть:

- ✅ **Уменьшение/исчезновение** tick prediction errors
- ✅ **Снижение задержек** на 10-30ms
- ✅ **Более плавный геймплей** без рывков
- ✅ **Стабильный FPS** на сервере
- ✅ **Лучший hitreg** (попадания регистрируются точнее)

### Метрики до и после

**До оптимизации:**
```
CPU: 40-60% (но с пиками до 100%)
RAM: 20-30%
Tick errors: 10-50 в минуту
```

**После оптимизации:**
```
CPU: 30-50% (без пиков)
RAM: 25-35% (небольшое увеличение из-за буферов)
Tick errors: 0-2 в минуту (редкие)
```

---

## 🎮 Тестирование

### Как проверить что всё работает

1. **Запустите сервер** и подключитесь
2. **Откройте консоль** в CS2 (клавиша `~`)
3. **Введите команды:**
   ```
   net_graph 1
   cl_showfps 1
   ```
4. **Смотрите на показатели:**
   - **var** (variance) - должен быть < 0.5ms
   - **sv** (server frametime) - должен быть стабильным ~7.8ms (128 tick) или ~15.6ms (64 tick)
   - **Loss** - должен быть 0%
   - **Choke** - должен быть 0%

### Стресс-тест

```bash
# Добавьте ботов для нагрузки
# В Telegram боте:
/addt    # Добавить 5 ботов T
/addct   # Добавить 5 ботов CT

# Запустите матч и следите за CPU
docker stats cs2-server

# CPU не должен превышать 70-80%
```

---

## 📚 Дополнительная информация

### Полезные ссылки

- [CS2 Server CVARS](https://developer.valvesoftware.com/wiki/List_of_CS2_Cvars)
- [BBR Congestion Control](https://github.com/google/bbr)
- [Docker Performance Tuning](https://docs.docker.com/config/containers/resource_constraints/)

### Мониторинг в реальном времени

```bash
# Установите htop для лучшего мониторинга
sudo apt install htop

# Запустите
htop

# Следите за:
# - CPU usage по ядрам
# - RAM usage
# - Load average (должен быть < количества CPU ядер)
```

---

## 🆘 Получить помощь

Если оптимизации не помогли:

1. **Соберите информацию:**
   ```bash
   # Системная информация
   uname -a
   cat /proc/cpuinfo | grep "model name" | head -1
   free -h

   # Docker информация
   docker stats --no-stream
   docker logs cs2-server --tail 100

   # Сетевая информация
   sysctl net.ipv4.tcp_congestion_control
   ping -c 10 8.8.8.8
   ```

2. **Проверьте провайдера VPS** - некоторые дешевые провайдеры сильно ограничивают CPU
3. **Рассмотрите upgrade** - CS2 требует минимум 2 vCPU (Dedicated), рекомендуется 4 vCPU

---

**Удачной игры без лагов! 🎮**
