# ⚡ Быстрая оптимизация CS2 сервера

## 🎯 3 шага для исправления лагов

### 1️⃣ Закоммитьте изменения

```bash
git add .
git commit -m "feat: Add CS2 server performance optimizations"
git push
```

### 2️⃣ На VPS запустите системную оптимизацию

```bash
ssh user@your-vps-ip
cd /path/to/project

# Скачайте изменения
git pull

# Запустите оптимизацию системы
sudo chmod +x optimize_system.sh
sudo ./optimize_system.sh
```

### 3️⃣ Деплой через Dokploy

Откройте панель Dokploy и нажмите **Deploy**

---

## ✅ Проверка результатов

```bash
# Подождите 1-2 минуты после деплоя, затем проверьте логи
docker logs cs2-server -f

# Следите за строками с [Prediction] - их должно стать меньше или исчезнуть
```

---

## 📊 Что было изменено

### Docker контейнер
- ✅ **network_mode: host** - сеть напрямую без Docker NAT (ключевое!)
- ✅ Выделено 4 CPU ядра
- ✅ Гарантировано 2GB RAM, максимум 4GB
- ✅ Оптимизированы сетевые буферы (16MB)
- ✅ Включен BBR congestion control
- ✅ Увеличены лимиты файлов и процессов

### CS2 конфиг
- ✅ 128 tick updaterate
- ✅ Параллельная обработка пакетов
- ✅ Улучшенная lag compensation
- ✅ Оптимизированная коррекция часов

### Система VPS
- ✅ Kernel параметры для сети
- ✅ BBR на уровне хоста
- ✅ Оптимизация Docker daemon
- ✅ Отключение transparent huge pages

---

## 🆘 Если не помогло

### Проверьте ресурсы

```bash
docker stats cs2-server
```

Если **CPU > 80%** постоянно - нужен более мощный VPS (минимум 4 vCPU Dedicated)

### Уменьшите tickrate

Откройте [docker-compose.prod.yml](docker-compose.prod.yml) и измените переменные окружения:

```yaml
- CS2_CFG_sv_maxupdaterate=64
- CS2_CFG_sv_minupdaterate=64
- CS2_CFG_sv_maxcmdrate=64
- CS2_CFG_sv_mincmdrate=64
```

Затем закоммитьте, пушьте и деплой снова.

---

## 📚 Подробная документация

Смотрите [PERFORMANCE_OPTIMIZATION.md](PERFORMANCE_OPTIMIZATION.md) для:
- Детального объяснения всех изменений
- Тонкой настройки под ваш VPS
- Диагностики и troubleshooting
- Мониторинга производительности

---

**Удачи! 🎮**
