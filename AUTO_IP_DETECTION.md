# 🌐 Автоматическое определение внешнего IP

## ✨ Что нового

Бот теперь **автоматически определяет внешний IP VPS** при запуске!

Больше не нужно вручную прописывать `CS2_IP` в `.env` файле. Бот сам определит внешний IP вашего сервера и покажет его в статусе.

---

## 🎯 Как это работает

### 1. При запуске бота

Когда контейнер бота запускается, происходит:

1. **Чтение .env файла**
   - Проверяются переменные `CS2_IP` и `CS2_DOMAIN`

2. **Автоматическое определение IP** (если не заданы вручную)
   - Бот обращается к внешним сервисам:
     - `https://api.ipify.org`
     - `https://ifconfig.me/ip`
     - `https://icanhazip.com`
     - `https://ipinfo.io/ip`
   - Пробует каждый сервис по очереди до успеха
   - Автоматически добавляет порт `:27015`

3. **Результат**
   - Если IP определен: `✓ Auto-detected external IP: 123.45.67.89:27015`
   - Если не определен: `⚠ Could not auto-detect external IP. Please set CS2_IP or CS2_DOMAIN in .env`

### 2. В Telegram боте

Команда `/start` → **📊 Server Status** покажет:

```
✅ Server is running

📛 Name: Captain CS2 Server
🔑 Password: 123456
🗺 Map: de_dust2
🎮 Mode: Competitive
🌐 IP: 123.45.67.89:27015

👥 Players (0):
- No players online

🚀 Quick Connect:
connect 123.45.67.89:27015; password 123456
```

---

## ⚙️ Конфигурация

### Вариант 1: Автоматическое определение (рекомендуется)

Оставьте пустыми в [.env](.env):

```env
CS2_IP=
CS2_DOMAIN=
```

Бот автоматически определит внешний IP при старте.

### Вариант 2: Ручная настройка (для особых случаев)

Укажите явно в [.env](.env):

```env
# Прямое подключение
CS2_IP=123.45.67.89:27015

# Или через Playit.gg
CS2_IP=69.9.185.19:25603
CS2_DOMAIN=hot-scott.deu.at.playit.plus:25603

# Или кастомный домен
CS2_DOMAIN=cs2.mydomain.com:27015
```

**Приоритет:**
1. Если задан `CS2_DOMAIN` - используется он
2. Если задан `CS2_IP` - используется он
3. Если оба пустые - автоматическое определение

---

## 🔧 Технические детали

### Изменения в файлах

#### 1. [bot/bot_config.py](bot/bot_config.py)

Добавлена функция `get_external_ip()`:

```python
import requests

def get_external_ip():
    """
    Автоматическое определение внешнего IP VPS.
    Пробует несколько сервисов для надежности.
    Возвращает IP:PORT или None если не удалось определить.
    """
    services = [
        "https://api.ipify.org",
        "https://ifconfig.me/ip",
        "https://icanhazip.com",
        "https://ipinfo.io/ip",
    ]

    for service in services:
        try:
            response = requests.get(service, timeout=5)
            if response.status_code == 200:
                ip = response.text.strip()
                return f"{ip}:27015"
        except Exception:
            continue

    return None

# Auto-detect IP if not set in .env
if not CS2_IP and not CS2_DOMAIN:
    try:
        detected_ip = get_external_ip()
        if detected_ip:
            CS2_IP = detected_ip
            print(f"✓ Auto-detected external IP: {CS2_IP}")
    except Exception as e:
        print(f"⚠ Error auto-detecting IP: {e}")
```

#### 2. [bot/requirements.txt](bot/requirements.txt)

Добавлена зависимость:

```
requests==2.31.0
```

#### 3. [.env.example](.env.example)

Обновлены комментарии:

```env
# Public Connection Info (Display only)
# OPTIONAL: Leave empty for automatic IP detection
# The bot will automatically detect your VPS external IP if these are not set
CS2_IP=
CS2_DOMAIN=
```

#### 4. [bot/handlers.py](bot/handlers.py)

Без изменений! Используется существующая логика:

```python
if config.CS2_IP:
    msg += f"🌐 **IP:** `{config.CS2_IP}`\n"
if config.CS2_DOMAIN:
    msg += f"🔗 **Domain:** `{config.CS2_DOMAIN}`\n"
```

---

## 📊 Примеры использования

### Сценарий 1: Чистая установка на новый VPS

```bash
# 1. Клонируйте репозиторий
git clone <your-repo>
cd cs2

# 2. Настройте .env (оставьте CS2_IP и CS2_DOMAIN пустыми)
cp .env.example .env
nano .env

# 3. Деплой через Dokploy или вручную
docker-compose -f docker-compose.prod.yml up -d

# 4. Проверьте логи - должны увидеть:
docker logs cs2-bot
# ✓ Auto-detected external IP: 123.45.67.89:27015
```

### Сценарий 2: Переезд на новый VPS

Ничего не нужно менять! Бот автоматически определит новый IP.

```bash
# На новом VPS просто запустите
docker-compose -f docker-compose.prod.yml up -d

# Бот определит новый IP автоматически
```

### Сценарий 3: Использование Playit.gg

Если прямое подключение не работает:

```bash
# 1. Запустите Playit.gg
docker-compose -f docker-compose.prod.yml up -d playit
docker logs cs2-playit  # Скопируйте claim URL

# 2. Настройте туннель в панели Playit.gg

# 3. Обновите .env
CS2_IP=69.9.185.19:25603
CS2_DOMAIN=hot-scott.deu.at.playit.plus:25603

# 4. Перезапустите бота
docker-compose -f docker-compose.prod.yml restart bot
```

---

## 🛠️ Устранение неполадок

### Проблема: IP не определяется

**Симптомы:** В логах видно:
```
⚠ Could not auto-detect external IP
```

**Решение 1:** Проверьте интернет-соединение

```bash
# Из контейнера бота
docker exec -it cs2-bot curl https://api.ipify.org
```

**Решение 2:** Задайте IP вручную

```env
CS2_IP=ВАШ_ВНЕШНИЙ_IP:27015
```

Узнать внешний IP:

```bash
curl https://api.ipify.org
```

### Проблема: Показывает неправильный IP

**Возможные причины:**
1. VPS за NAT
2. Используется прокси
3. VPS имеет несколько сетевых интерфейсов

**Решение:** Задайте IP вручную в `.env`

### Проблема: Хочу использовать домен вместо IP

**Решение:**

```env
CS2_DOMAIN=cs2.mydomain.com:27015
CS2_IP=  # Оставить пустым или удалить
```

---

## ✅ Преимущества

- ✅ **Автоматизация** - не нужно вручную прописывать IP
- ✅ **Переносимость** - работает на любом VPS без настройки
- ✅ **Надежность** - пробует несколько сервисов определения IP
- ✅ **Гибкость** - можно задать IP вручную если нужно
- ✅ **Совместимость** - работает с Playit.gg и кастомными доменами

---

## 🚀 Следующие шаги

После настройки автоматического определения IP:

1. **Закоммитьте изменения**
   ```bash
   git add .
   git commit -m "feat: Add automatic external IP detection"
   git push
   ```

2. **Деплой через Dokploy**
   - Нажмите Deploy в панели

3. **Проверьте работу**
   ```bash
   # Логи бота
   docker logs cs2-bot

   # Должны увидеть:
   # ✓ Auto-detected external IP: YOUR_IP:27015
   ```

4. **Проверьте в Telegram**
   - `/start` → **📊 Server Status**
   - Должен показывать реальный IP

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте логи: `docker logs cs2-bot -f`
2. Проверьте `.env`: убедитесь, что `CS2_IP` и `CS2_DOMAIN` пустые
3. Проверьте интернет в контейнере: `docker exec -it cs2-bot curl https://api.ipify.org`

**Не работает?** Задайте IP вручную в `.env`:

```env
CS2_IP=ВАШ_IP:27015
```
