# 🎮 Captain CS2 Bot

![CS2](https://img.shields.io/badge/CS2-Server-orange)
![Python](https://img.shields.io/badge/Python-3.11+-blue)
![Docker](https://img.shields.io/badge/Docker-Compose-blue)
![Telegram](https://img.shields.io/badge/Telegram-Bot-blue)

Telegram-бот для удаленного управления выделенным сервером Counter-Strike 2 через RCON протокол.

## 📋 Содержание

- [Описание](#описание)
- [Возможности](#возможности)
- [Архитектура](#архитектура)
- [Требования](#требования)
- [Установка](#установка)
- [Конфигурация](#конфигурация)
- [Использование](#использование)
- [Команды бота](#команды-бота)
- [Разработка](#разработка)
- [Устранение неполадок](#устранение-неполадок)
- [Лицензия](#лицензия)

## 📖 Описание

**Captain CS2 Bot** — это Telegram-бот, предназначенный для удобного управления выделенным сервером Counter-Strike 2. Бот использует RCON (Remote Console) протокол для отправки команд на игровой сервер и предоставляет интуитивный интерфейс с инлайн-кнопками для управления картами, режимами игры, ботами и игроками.

Проект разработан для запуска на домашнем сервере с использованием Docker и включает в себя:
- **CS2 сервер** (на базе [kus/cs2-modded-server](https://github.com/kus/cs2-modded-server))
- **Telegram Bot** (Python + aiogram)
- Возможность использования **Cloudflare Tunnel** для внешнего доступа

## ✨ Возможности

### 🗺️ Управление картами
- Смена карт через удобное меню с инлайн-кнопками
- Поддержка популярных карт:
  - de_dust2, de_mirage, de_inferno
  - de_nuke, de_anubis, cs_office
  - и многие другие

### 🎯 Управление режимами игры
- **Casual** (Обычный режим)
- **Competitive** (Соревновательный)
- **Deathmatch**
- **Wingman** (Напарники)

### 🤖 Управление ботами
- Добавление ботов за террористов (T)
- Добавление ботов за контртеррористов (CT)
- Удаление всех ботов с сервера
- Настройка сложности ботов

### 👥 Управление игроками
- Просмотр списка игроков на сервере
- Кик игроков
- Просмотр статуса сервера

### 🔒 Безопасность
- Ограничение доступа по Telegram User ID
- Безопасное хранение конфигурации в `.env` файле

## 🏗️ Архитектура

Система состоит из трех ключевых компонентов:

```
┌─────────────────┐      RCON       ┌──────────────────┐
│  Telegram Bot   │ ◄───────────► │   CS2 Server     │
│   (Python)      │   протокол     │  (Docker)        │
└─────────────────┘                 └──────────────────┘
        ▲                                    │
        │                                    │
        │                                    ▼
   ┌────┴─────┐                     ┌────────────────┐
   │  User    │                     │  Game Clients  │
   │ (Telegram)│                     │   (Players)    │
   └──────────┘                     └────────────────┘
```

### Компоненты:

1. **CS2 Server Container**
   - Базовый образ: `ghcr.io/kus/cs2-modded-server:latest`
   - RCON порт: 27015 (TCP)
   - Игровой порт: 27015 (UDP)

2. **Telegram Bot Container**
   - Python 3.11+
   - Библиотеки: aiogram, python-valve
   - Взаимодействие с CS2 через RCON

3. **Cloudflare Tunnel** (опционально)
   - Для безопасного доступа извне
   - Альтернативы: ZeroTier, Tailscale

## 💻 Требования

### Системные требования:
- **ОС**: Windows/Linux/macOS
- **ОЗУ**: минимум 6 ГБ
- **Диск**: минимум 50 ГБ свободного пространства
- **Docker**: 20.10+
- **Docker Compose**: 2.0+

### Необходимые токены и ключи:
1. **Steam Game Server Login Token (GSLT)**
   - Получить на: https://steamcommunity.com/dev/managegameservers
   
2. **Telegram Bot Token**
   - Создать бота через [@BotFather](https://t.me/BotFather)
   
3. **Telegram User ID**
   - Узнать свой ID через [@userinfobot](https://t.me/userinfobot)

4. **Steam API Key** (опционально)
   - Получить на: https://steamcommunity.com/dev/apikey

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/MasterinoSplinterino/Captain_CS2_bot.git
cd Captain_CS2_bot
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корневой директории проекта:

```bash
# Скопируйте пример конфигурации
cp .env.example .env
```

Заполните `.env` файл:

```env
# Steam Configuration
CS2_GSLT_TOKEN=your_gslt_token_here
STEAM_API_KEY=your_steam_api_key_here

# CS2 Server Configuration
CS2_PASSWORD=your_server_password
CS2_SERVERNAME=My Awesome CS2 Server
RCON_PASSWORD=your_strong_rcon_password

# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
ALLOWED_USER_ID=your_telegram_user_id
```

### 3. Запуск через Docker Compose

```bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка сервисов
docker-compose down
```

### 4. Первый запуск CS2 сервера

При первом запуске CS2 сервер скачает все необходимые файлы (~30-40 ГБ). Это может занять от 30 минут до нескольких часов в зависимости от скорости интернета.

Следите за прогрессом:
```bash
docker-compose logs -f cs2
```

## ⚙️ Конфигурация

### Структура проекта

```
Captain_CS2_bot/
├── bot/                          # Код Telegram бота
│   ├── main.py                   # Точка входа
│   ├── bot_config.py             # Конфигурация
│   ├── handlers.py               # Обработчики команд
│   ├── keyboards.py              # Клавиатуры
│   ├── rcon_client.py            # RCON клиент
│   ├── debug_rcon.py             # Утилита отладки RCON
│   ├── requirements.txt          # Python зависимости
│   └── Dockerfile                # Docker образ бота
├── cs2-data/                     # Данные CS2 сервера (создается автоматически)
│   ├── game/                     # Игровые файлы
│   └── csgo/                     # Конфигурация
├── docker-compose.yml            # Конфигурация Docker Compose
├── .env                          # Переменные окружения (не в репозитории!)
├── .gitignore                    # Исключения для Git
├── README.md                     # Этот файл
├── 01_base.md                    # Техническое задание
├── 02_project_plan.md            # План реализации
├── README_cs2Server_info.md      # Информация о CS2 сервере
├── update_map_data.py            # Скрипт обновления данных карт
└── maps_by_mode.txt              # Список карт по режимам
```

### Docker Compose конфигурация

Файл `docker-compose.yml` определяет два сервиса:

#### CS2 Server
```yaml
services:
  cs2:
    image: ghcr.io/kus/cs2-modded-server:latest
    container_name: cs2-server
    restart: unless-stopped
    ports:
      - "27015:27015/tcp"  # RCON
      - "27015:27015/udp"  # Game
      - "27020:27020/udp"  # SourceTV
    environment:
      - SRCDS_TOKEN=${CS2_GSLT_TOKEN}
      - SRCDS_PW=${CS2_PASSWORD}
      - SRCDS_RCONPW=${RCON_PASSWORD}
      - SRCDS_HOSTNAME=${CS2_SERVERNAME}
    volumes:
      - ./cs2-data:/home/steam/cs2/
```

#### Telegram Bot
```yaml
  bot:
    build: ./bot
    container_name: cs2-bot
    restart: unless-stopped
    environment:
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - ALLOWED_USER_ID=${ALLOWED_USER_ID}
      - RCON_HOST=cs2
      - RCON_PORT=27015
      - RCON_PASSWORD=${RCON_PASSWORD}
    depends_on:
      - cs2
```

## 📱 Использование

### Запуск бота в Telegram

1. Найдите вашего бота в Telegram по имени, которое вы указали при создании через @BotFather
2. Отправьте команду `/start`
3. Бот приветствует вас и показывает доступные команды

### Основной интерфейс

Бот предоставляет удобное меню с кнопками:
- 🗺️ **Сменить карту**
- 🎮 **Сменить режим**
- 🤖 **Управление ботами**
- 👥 **Управление игроками**
- ℹ️ **Статус сервера**

## 🎮 Команды бота

### Основные команды

| Команда | Описание |
|---------|----------|
| `/start` | Приветствие и главное меню |
| `/help` | Список всех доступных команд |
| `/status` | Показать статус сервера и список игроков |

### Управление картами

| Команда | Описание |
|---------|----------|
| `/map` | Открыть меню выбора карт |

**Доступные карты** (примеры):
- de_dust2, de_mirage, de_inferno
- de_nuke, de_anubis, de_vertigo
- de_ancient, de_overpass, de_cache
- cs_office, cs_italy

### Управление режимами игры

| Команда | Описание |
|---------|----------|
| `/mode` | Открыть меню выбора режима игры |

**Доступные режимы:**
- 🎯 **Casual** - Обычный режим (game_type 0, game_mode 0)
- 🏆 **Competitive** - Соревновательный (game_type 0, game_mode 1)
- ⚔️ **Deathmatch** - Смертельный бой (game_type 2, game_mode 0)
- 👥 **Wingman** - Напарники (game_type 3, game_mode 0)

### Управление ботами

| Команда | Параметры | Описание |
|---------|-----------|----------|
| `/addt` | [сложность] | Добавить бота за террористов |
| `/addct` | [сложность] | Добавить бота за контртеррористов |
| `/removebots` | - | Удалить всех ботов с сервера |

**Уровни сложности ботов:**
- `easy` - Легкий
- `normal` - Средний
- `hard` - Сложный
- `expert` - Эксперт

**Примеры:**
```
/addt easy       # Добавить легкого бота-террориста
/addct expert    # Добавить эксперта-контртеррориста
/removebots      # Убрать всех ботов
```

### Управление игроками

| Команда | Параметры | Описание |
|---------|-----------|----------|
| `/kick` | <имя игрока> | Кикнуть игрока с сервера |
| `/players` | - | Показать список игроков |

**Примеры:**
```
/kick PlayerName    # Кикнуть игрока с ником PlayerName
/players           # Показать всех игроков
```

### Административные команды

| Команда | Описание |
|---------|----------|
| `/restart` | Перезапустить текущий матч |
| `/rcon <команда>` | Выполнить произвольную RCON команду |

**Примеры:**
```
/restart                    # Перезапуск матча
/rcon mp_restartgame 1      # Перезапуск через 1 секунду
/rcon sv_cheats 1           # Включить читы (для практики)
```

## 🛠️ Разработка

### Структура кода бота

#### main.py
Точка входа приложения. Инициализирует бота и диспетчер, запускает polling.

```python
import asyncio
from aiogram import Bot, Dispatcher
from bot_config import TELEGRAM_BOT_TOKEN
from handlers import router

async def main():
    bot = Bot(token=TELEGRAM_BOT_TOKEN)
    dp = Dispatcher()
    dp.include_router(router)
    await dp.start_polling(bot)

if __name__ == "__main__":
    asyncio.run(main())
```

#### bot_config.py
Загружает конфигурацию из переменных окружения и определяет списки карт и режимов.

**Основные переменные:**
- `TELEGRAM_BOT_TOKEN` - токен Telegram бота
- `ALLOWED_USER_ID` - список разрешенных User ID
- `RCON_HOST`, `RCON_PORT`, `RCON_PASSWORD` - параметры подключения к RCON

#### handlers.py
Обрабатывает все команды и callback-запросы от пользователей.

**Основные handlers:**
- `cmd_start()` - обработка команды /start
- `cmd_map()` - показ меню выбора карт
- `map_callback()` - обработка выбора карты
- `cmd_mode()` - показ меню выбора режима
- `mode_callback()` - обработка выбора режима

#### keyboards.py
Создает инлайн-клавиатуры для интерактивного управления.

**Основные функции:**
- `get_maps_keyboard()` - клавиатура выбора карт
- `get_modes_keyboard()` - клавиатура выбора режимов
- `get_main_menu()` - главное меню

#### rcon_client.py
Обертка для работы с RCON протоколом через библиотеку python-valve.

**Основные методы:**
- `execute_command(cmd)` - выполнение RCON команды
- `change_map(map_name)` - смена карты
- `change_mode(game_type, game_mode, map_name)` - смена режима игры
- `get_status()` - получение статуса сервера

### Добавление новых карт

Откройте файл `bot/bot_config.py` и добавьте карты в соответствующий список:

```python
MAPS = {
    "Competitive": [
        "de_dust2",
        "de_mirage",
        "de_your_new_map"  # Добавьте сюда
    ]
}
```

### Добавление новых команд

1. Создайте handler в `handlers.py`:
```python
@router.message(Command("yourcommand"))
async def cmd_yourcommand(message: types.Message):
    # Ваша логика
    await message.answer("Ответ бота")
```

2. Добавьте описание в меню помощи

### Локальная разработка

Для разработки без Docker:

```bash
# Установка зависимостей
cd bot
pip install -r requirements.txt

# Создайте .env в папке bot/
# Запуск
python main.py
```

### Отладка RCON

Используйте утилиту `debug_rcon.py` для тестирования RCON подключения:

```bash
python bot/debug_rcon.py
```

## 🔧 Устранение неполадок

### Бот не отвечает

**Проверьте:**
1. Контейнер запущен: `docker ps`
2. Логи бота: `docker-compose logs bot`
3. Токен бота правильный: проверь `.env`
4. User ID в списке разрешенных

### RCON не подключается

**Проверьте:**
1. CS2 сервер запущен: `docker ps | grep cs2`
2. RCON пароль совпадает в обоих сервисах
3. Порт 27015/tcp открыт
4. Логи сервера: `docker-compose logs cs2`

**Тест RCON вручную:**
```bash
# Войдите в контейнер бота
docker exec -it cs2-bot bash

# Запустите debug скрипт
python debug_rcon.py
```

### CS2 сервер не запускается

**Проверьте:**
1. GSLT токен валидный (Steam Game Server Login Token)
2. Достаточно места на диске (минимум 50 ГБ)
3. Порты не заняты другими приложениями
4. Логи: `docker-compose logs cs2`

**Типичные проблемы:**
- Неверный GSLT токен → создайте новый на Steam
- Недостаточно RAM → минимум 6 ГБ
- Порт 27015 занят → измените в docker-compose.yml

### Карты не загружаются

**Проверьте:**
1. Карты установлены в `cs2-data/game/csgo/maps/`
2. Имена карт правильные (например, `de_dust2`, не `dust2`)
3. Сервер скачал все воркшопные карты

### Игроки не могут подключиться

**Проверьте:**
1. Порты проброшены на роутере (27015 UDP/TCP)
2. Firewall разрешает подключения
3. CS2_PASSWORD совпадает
4. Cloudflare Tunnel настроен правильно (если используется)

### Полезные команды для диагностики

```bash
# Статус всех контейнеров
docker-compose ps

# Логи всех сервисов
docker-compose logs -f

# Перезапуск всех сервисов
docker-compose restart

# Полная очистка и пересборка
docker-compose down
docker-compose up -d --build

# Проверка RCON изнутри контейнера
docker exec -it cs2-bot python debug_rcon.py

# Вход в консоль CS2 сервера
docker exec -it cs2-server bash
```

## 📊 Мониторинг

### Просмотр статистики сервера

```bash
# Использование ресурсов
docker stats

# Логи в реальном времени
docker-compose logs -f cs2

# Проверка места на диске
du -sh cs2-data/
```

### Бэкап данных сервера

```bash
# Создание бэкапа
tar -czf cs2-backup-$(date +%Y%m%d).tar.gz cs2-data/

# Восстановление из бэкапа
tar -xzf cs2-backup-20241129.tar.gz
```

## 🚀 Продакшен деплой

### Рекомендации для продакшена

1. **Используйте .env файл** - не храните секреты в коде
2. **Настройте автообновление** - keep CS2 server updated
3. **Мониторинг** - используйте Prometheus + Grafana
4. **Логирование** - настройте централизованное логирование
5. **Бэкапы** - регулярно делайте бэкапы cs2-data
6. **Rate limiting** - защита от спама команд

### Cloudflare Tunnel

Для доступа извне без проброса портов:

```bash
# Установка cloudflared
wget https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb
sudo dpkg -i cloudflared-linux-amd64.deb

# Авторизация
cloudflared tunnel login

# Создание туннеля
cloudflared tunnel create cs2-server

# Настройка конфигурации
cloudflared tunnel route dns cs2-server cs2.yourdomain.com
```

## 🤝 Вклад в проект

Вы можете помочь проекту:
1. 🐛 Сообщить об ошибке (Issues)
2. 💡 Предложить новую функцию
3. 🔧 Создать Pull Request
4. 📖 Улучшить документацию

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Смотрите файл `LICENSE` для подробностей.

## 🙏 Благодарности

- [kus/cs2-modded-server](https://github.com/kus/cs2-modded-server) - за отличный Docker образ CS2 сервера
- [aiogram](https://github.com/aiogram/aiogram) - за удобную библиотеку для Telegram ботов
- [python-valve](https://github.com/serverstf/python-valve) - за RCON клиент

## 📞 Контакты

- **GitHub**: [MasterinoSplinterino](https://github.com/MasterinoSplinterino)
- **Telegram**: [@Captain_cs2_bot](https://t.me/Captain_cs2_bot)

---

**Удачной игры! 🎮**
